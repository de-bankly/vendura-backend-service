/**
 * This module is about user management and authentication of users with JWT
 *
 * <p>The authentication package manages user and role data, handles JWT-based authentication, and
 * ensures secure authorization through role checks. It includes error handling for invalid JWTs, a
 * security filter for token validation, and integrates with Spring Security via SecurityConfig and
 * UserDetailsService.
 *
 * @author Anton Bowe
 */
package com.bankly.vendura.authentication;
